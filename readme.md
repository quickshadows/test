--- установка перед использованием тестов.

sudo apt update && sudo apt install sysbench -y

git clone https://github.com/quickshadows/test.git

cd test

sudo apt install python3-pip -y

pip install -r requirements.txt
